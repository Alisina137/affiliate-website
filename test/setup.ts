// Shared Vitest setup.
//
// Keep this file intentionally lightweight. Individual test suites should
// register mocks/hooks they actually need instead of imposing browser or
// database state on every test.
export {}
